<%
  # locals: {
  #   resource: "the resource to generate actions for",
  #   actions: "list of actions to generate",
  # }
%>
<% actions.values.each do |action| %>
  <% if action.button.is_a? Plutonium::UI::Button %>
    <%=
      table_action_button adapt_route_args(resource, action: action.route.action, **action.route.options),
                        icon: action.button.icon,
                        button_class: action.button.button_class,
                        method: action.route.method,
                        confirm: action.confirmation
    %>
  <% end %>
<% end %>
