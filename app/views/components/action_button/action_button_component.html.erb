<% if method == :get %>
  <%= resource_component :button, to:, icon:, label:, size:,
                                       color:, variant:, classname:, data: {turbo_frame:} %>
<% else %>
  <%= form_for :action, url: to, method:, html: {class: classname, data: {turbo_confirm: confirmation, turbo_frame:}} do %>
    <%= resource_component :button, type: :submit, icon:, label:, size:,
                                    color:, variant:, classname: "w-full" %>
  <% end %>
<% end %>
