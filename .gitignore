/.bundle/
/.yardoc
/_yardoc/
/coverage/
/doc/
/pkg/
/spec/reports/
/tmp/

# rspec failure tracking
.rspec_status

.DS_Store
node_modules
build
dist

ai_tools
test/internal/db/*.sqlite*

docs/.vitepress/cache
docs/.vitepress/dist

Gemfile.lock

log/
test/dummy/log/*.log
test/dummy/storage/*
test/dummy/tmp
test/dummy/public/uploads/*
# Built from migrations, never committed (dropped in 62d23940). It reappears
# every time anyone runs db:migrate, so without this a stray `git add -A` puts
# it back — and a committed schema then fights whichever migrations a branch
# happens to add.
test/dummy/db/schema.rb

.yarn

.superpowers

*.sqlite*
docs/public/skills
