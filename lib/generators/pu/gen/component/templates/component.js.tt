import { Controller } from "@hotwired/stimulus"

// Connects to data-controller="<%= component_controller %>"
export default class extends Controller {
  connect() {
    console.log(`<%= component_controller %> connected: ${this.element}`)
  }
}
